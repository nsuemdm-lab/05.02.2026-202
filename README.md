
# ﻿05.02.2026
Группа: 9-ИС202 Дата: 05 февраля 2026 г. (Четверг)
Формат: Очный (Аудитория 5-517).
________________


ОБЩАЯ КОНЦЕПЦИЯ ДНЯ: «ПОЛНЫЙ КОНТРОЛЬ (CRUD COMPLETE)»
Статус:
1.  Мы умеем Создавать (Create), Читать (Read), Искать и Загружать файлы.
2.  Проблема: Если Админ ошибся в цене товара, ему приходится удалять товар и создавать заново. Если товаров станет 1000, страница будет грузиться вечность.
3.  Задача дня: Завершить цикл CRUD (Update/Delete) и реализовать Пагинацию (постраничный вывод).
________________


11:25 | ПАРА №1. ПМ.09. Редактирование данных (Update)
Тема: Создание интерфейса редактирования сущностей.
ВВЕДЕНИЕ:
«Редактирование — это самое сложное в CRUD. Вам нужно: 
а) Получить старые данные, 
б) Показать их в форме, 
в) Сохранить изменения. 

Ошибка здесь часто приводит к тому, что все товары становятся одинаковыми (забыли `WHERE id=...`).»

ИНСТРУКЦИЯ:
ШАГ 1. Ссылка на редактирование
В админ-панели или в личном кабинете добавьте кнопку «Редактировать»:
`<a href="edit_item.php?id=<?= $item['id'] ?>" class="btn btn-warning">✏️</a>`

ШАГ 2. Форма с предзаполнением (`edit_item.php`)
Код похож на `add_item.php`, но сначала мы делаем `SELECT`.

```php
<?php
require 'db.php';
require 'check_admin.php'; // Защита!

$id = (int)$_GET['id'];
$stmt = $pdo->prepare("SELECT * FROM products WHERE id = ?");
$stmt->execute([$id]);
$product = $stmt->fetch();

if (!$product) die("Товар не найден");
?>

<!-- ВАЖНО: В value подставляем старые данные -->
<form method="POST">
    <input type="text" name="title" value="<?= htmlspecialchars($product['title']) ?>">
    <input type="number" name="price" value="<?= $product['price'] ?>">
    <button type="submit">Обновить</button>
</form>
```

ШАГ 3. Обработка сохранения (UPDATE)
В том же файле (сверху):
```php
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $sql = "UPDATE products SET title = ?, price = ? WHERE id = ?";
    $stmt = $pdo->prepare($sql);
    $stmt->execute([$_POST['title'], $_POST['price'], $id]);
    echo "Обновлено!";
}
```

ЗАДАНИЕ НА ПАРУ:
Реализовать скрипт `edit_*.php` для своей основной сущности. Проверить, что меняется именно та запись, которую выбрали.
________________


13:20 | ПАРА №2. МДК.09.03. Безопасное удаление (Delete)
Тема: Удаление данных и защита от CSRF.
ВВЕДЕНИЕ (Methodist_Sec):
«Никогда, слышите, НИКОГДА не делайте удаление через ссылку `<a href="delete.php?id=5">`. Поисковый робот Google перейдет по всем ссылкам на сайте и удалит все ваши товары. Удаление — это только метод POST!»

ИНСТРУКЦИЯ:
ШАГ 1. Форма удаления (Вместо ссылки)
Вставляем это в таблицу товаров/записей:
```html
<form action="delete_item.php" method="POST" onsubmit="return confirm('Вы уверены?');">
    <input type="hidden" name="id" value="<?= $item['id'] ?>">
    <!-- CSRF токен обязателен! (см. урок от 29.01) -->
    <input type="hidden" name="csrf_token" value="<?= $_SESSION['csrf_token'] ?>">
    <button type="submit" class="btn btn-danger btn-sm">🗑️ Удалить</button>
</form>
```

ШАГ 2. Скрипт удаления (`delete_item.php`)
```php
<?php
session_start();
require 'db.php';
require 'check_admin.php';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    // 1. Проверка токена
    if (!hash_equals($_SESSION['csrf_token'], $_POST['csrf_token'])) {
        die("CSRF Attack blocked");
    }

    // 2. Удаление
    $id = (int)$_POST['id'];
    $stmt = $pdo->prepare("DELETE FROM products WHERE id = ?");
    $stmt->execute([$id]);
    
    header("Location: index.php");
}
?>
```

ЗАДАНИЕ НА ПАРУ:
Реализовать удаление записей. Добавить JavaScript-подтверждение `confirm()`.
________________


15:05 | ПАРА №3. ПМ.09. Пагинация (Pagination)
Тема: Постраничный вывод данных.
ВВЕДЕНИЕ:
«Выводить 1000 товаров сразу — это убить браузер клиента. Мы будем выводить по 10 штук. Для этого в SQL есть команды `LIMIT` (сколько взять) и `OFFSET` (сколько пропустить).»

МАТЕМАТИКА:
`$page` = Номер страницы (из URL).
`$limit` = 10 (записей на страницу).
`$offset` = `($page - 1) * $limit`.

ИНСТРУКЦИЯ:
ШАГ 1. Расчеты (в `index.php`)
```php
$page = isset($_GET['page']) ? (int)$_GET['page'] : 1;
$limit = 10;
$offset = ($page - 1) * $limit;

// Получаем общее кол-во товаров (для кнопок 1, 2, 3...)
$total_stmt = $pdo->query("SELECT COUNT(*) FROM products");
$total_rows = $total_stmt->fetchColumn();
$total_pages = ceil($total_rows / $limit);
```

ШАГ 2. SQL запрос
```php
// Добавляем в конец вашего основного запроса
$sql = "SELECT * FROM products LIMIT $limit OFFSET $offset";
// ... выполнение ...
```

ШАГ 3. Кнопки навигации (HTML)
```html
<nav>
  <ul class="pagination">
    <?php for ($i = 1; $i <= $total_pages; $i++): ?>
      <li class="page-item <?= ($i == $page) ? 'active' : '' ?>">
        <a class="page-link" href="?page=<?= $i ?>"><?= $i ?></a>
      </li>
    <?php endfor; ?>
  </ul>
</nav>
```

ЗАДАНИЕ НА ПАРУ:
Реализовать пагинацию. Проверить: если товаров 15, а лимит 10, должно быть 2 страницы.
________________


ИНДИВИДУАЛЬНЫЕ ВЕКТОРЫ РАЗВИТИЯ (КУРСОВЫЕ ПРОЕКТЫ)

Теперь, когда у вас есть "Full CRUD", работа над функционалом завершается. Следующий этап — **Бизнес-логика и "Фишки"**.

**1. Группа "Магазины и Каталоги" (Вотинцев, Мартынова, Тонковид, Шкода, Маркварт)**
*   **Задача:** Реализовать "Корзину" (Cart) на сессиях, если еще нет.
*   **Update:** Редактирование количества товара в корзине.
*   **Delete:** Удаление товара из корзины.
*   **Фишка:** Подсчет итоговой суммы в реальном времени.

**2. Группа "Запись и Бронирование" (Дмитриев, Сердюкова, Кудрявцев, Орлова)**
*   **Задача:** Календарь занятости.
*   **Update:** Перенос записи (смена даты).
*   **Delete:** Отмена брони (с проверкой: нельзя отменить, если осталось меньше 24 часов).
*   **Фишка:** Визуальная сетка занятых слотов (красный - занято, зеленый - свободно).

**3. Группа "Социальные и HR" (Агапов, Попова, Постнова, Белозерцева, Рогова)**
*   **Задача:** Статусная модель.
*   **Update:** Смена статуса заявки (Новая -> В работе -> Завершена).
*   **Delete:** Архивация (Soft Delete). Не удалять из БД физически, а ставить `is_deleted = 1`.
*   **Фишка:** Уведомления (хотя бы заглушки) при смене статуса.

**4. Группа "Учет и Инвентаризация" (Залуцкий, Лещинский, Торшин, Ибрагимов)**
*   **Задача:** Складской учет / История изменений.
*   **Update:** При изменении количества товара (приход/расход) записывать лог в отдельную таблицу `history`.
*   **Delete:** Списание оборудования (с указанием причины).
*   **Фишка:** Генерация отчета (простая HTML таблица для печати).

**5. Группа "Гео и Поиск" (Барабашов, Великанов, Сенеджук)**
*   **Задача:** Работа с координатами (или адресами).
*   **Update:** Изменение адреса объекта.
*   **Delete:** Снятие объявления с публикации (скрытие).
*   **Фишка:** Интеграция виджета Яндекс.Карт (статического) в карточку объекта.

________________


ДОМАШНЕЕ ЗАДАНИЕ:
1.  Привести в порядок все формы (использовать Bootstrap классы `form-control`, `btn`).
2.  Убедиться, что Админка полностью функциональна: Админ может добавить, найти, изменить и удалить любую запись.
3.  **Подготовка к следующему занятию:** Начать писать Пояснительную записку. Скачать шаблон ПЗ.

--- END OF FILE ---
